    <!-- Delete Modal -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModal" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content bg-light">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Confirmez la suppression</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body my-3 text-end">
                    <button type="button" class="btn btn-outline-danger shadow" data-bs-dismiss="modal">Annuler</button>
                    <a class="btn btn-outline-info shadow ms-2"
                        href="{{ path('app_application_delete', {'id': application.id}) }}">Supprimer</a>
                </div>
            </div>
        </div>
    </div>
